---
description: Архитектурные принципы и структура проекта. Применяется при создании новых проектов и модулей.
globs: ["**/*"]
alwaysApply: false
---

# АРХИТЕКТУРНЫЕ ПРИНЦИПЫ

> Применяй эти правила при проектировании структуры проекта и принятии архитектурных решений.

---

## 🏗️ АРХИТЕКТУРНЫЕ ПАТТЕРНЫ

### Clean Architecture (по умолчанию)

```
┌─────────────────────────────────────┐
│     Presentation (UI/API)           │  ← Фреймворк-зависимый слой
│     - React компоненты              │
│     - API контроллеры               │
│     - Middleware                    │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Application (Use Cases)         │  ← Бизнес-сценарии
│     - Сервисы                       │
│     - Команды/Запросы               │
│     - DTO                           │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Domain (Entities)               │  ← Чистая бизнес-логика
│     - Сущности                      │
│     - Value Objects                 │
│     - Бизнес-правила                │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Infrastructure                  │  ← Внешние зависимости
│     - База данных                   │
│     - Внешние API                   │
│     - Файловая система              │
└─────────────────────────────────────┘
```

### Правила зависимостей
- Внутренние слои НЕ знают о внешних
- Domain НЕ импортирует из Application/Infrastructure
- Application НЕ импортирует из Presentation
- Зависимости идут ТОЛЬКО внутрь

---

## 📁 СТРУКТУРА ПРОЕКТА ПО РАЗМЕРУ

### 🟢 Малый проект (A) — Monolith Simple

```
project/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (routes)/          # Группы маршрутов
│   │   ├── api/               # API routes
│   │   └── layout.tsx
│   │
│   ├── components/            # UI компоненты
│   │   ├── ui/               # Базовые (Button, Input)
│   │   └── features/         # Бизнес-компоненты
│   │
│   ├── lib/                   # Утилиты и хелперы
│   │   ├── utils/
│   │   ├── hooks/
│   │   └── api-client.ts
│   │
│   ├── types/                 # TypeScript типы
│   │
│   └── styles/                # Глобальные стили
│
├── prisma/                    # Схема БД
│   └── schema.prisma
│
├── public/                    # Статика
│
├── docs/                      # Документация
│   ├── 01-ARCHITECTURE.md
│   └── PROGRESS.md
│
├── .env.example
├── package.json
└── README.md
```

---

### 🟡 Средний проект (B) — Feature-Based

```
project/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (marketing)/       # Публичные страницы
│   │   ├── (dashboard)/       # Защищённые страницы
│   │   ├── api/               # API routes
│   │   └── layout.tsx
│   │
│   ├── features/              # Фичи (главная структура!)
│   │   ├── auth/
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   ├── services/
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── products/
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   ├── services/
│   │   │   ├── types.ts
│   │   │   └── index.ts
│   │   │
│   │   └── orders/
│   │       └── ...
│   │
│   ├── shared/                # Общий код
│   │   ├── components/        # Переиспользуемые компоненты
│   │   │   ├── ui/           # Базовые (Button, Modal)
│   │   │   └── layout/       # Layout компоненты
│   │   ├── hooks/            # Общие хуки
│   │   ├── lib/              # Утилиты
│   │   ├── types/            # Общие типы
│   │   └── constants/        # Константы
│   │
│   └── config/               # Конфигурация
│       ├── env.ts
│       └── constants.ts
│
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
│
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
│
├── docs/
│   ├── 01-ARCHITECTURE.md
│   ├── 02-TECH_STACK.md
│   ├── 03-API.md
│   ├── PROGRESS.md
│   └── DECISIONS.md
│
├── scripts/                   # Вспомогательные скрипты
│
└── ...config files
```

---

### 🔴 Крупный проект (C) — Monorepo / Modular

```
project/
├── apps/
│   ├── web/                   # Frontend (Next.js)
│   │   ├── src/
│   │   │   ├── app/
│   │   │   ├── features/
│   │   │   └── shared/
│   │   └── package.json
│   │
│   ├── api/                   # Backend (NestJS)
│   │   ├── src/
│   │   │   ├── modules/
│   │   │   │   ├── auth/
│   │   │   │   ├── users/
│   │   │   │   └── products/
│   │   │   ├── common/
│   │   │   └── main.ts
│   │   └── package.json
│   │
│   └── admin/                 # Admin panel (опционально)
│
├── packages/
│   ├── ui/                    # UI библиотека
│   │   ├── src/
│   │   │   ├── Button/
│   │   │   ├── Modal/
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── domain/                # Бизнес-логика
│   │   ├── src/
│   │   │   ├── entities/
│   │   │   ├── use-cases/
│   │   │   └── rules/
│   │   └── package.json
│   │
│   ├── shared/                # Общие утилиты
│   │   ├── src/
│   │   │   ├── utils/
│   │   │   ├── types/
│   │   │   └── constants/
│   │   └── package.json
│   │
│   └── config/                # Общие конфиги
│       ├── eslint/
│       ├── typescript/
│       └── tailwind/
│
├── infrastructure/
│   ├── docker/
│   ├── kubernetes/
│   └── terraform/
│
├── docs/
│   ├── architecture/
│   │   ├── 01-OVERVIEW.md
│   │   ├── 02-MODULES.md
│   │   └── diagrams/
│   ├── decisions/
│   │   ├── ADR-001-monorepo.md
│   │   └── ADR-002-...
│   ├── api/
│   └── guides/
│
├── scripts/
├── turbo.json                 # Turborepo
├── pnpm-workspace.yaml
└── package.json
```

---

## 🔧 ПРАВИЛА МОДУЛЕЙ

### Feature Module структура

```typescript
// features/products/index.ts - публичный API модуля

// Экспортируй только то, что нужно снаружи
export { ProductCard } from './components/ProductCard';
export { ProductList } from './components/ProductList';
export { useProducts } from './hooks/useProducts';
export { productService } from './services/productService';
export type { Product, ProductFilters } from './types';

// НЕ экспортируй внутренние детали
// - внутренние хелперы
// - приватные компоненты
// - internal types
```

### Правила импортов

```typescript
// ✅ ПРАВИЛЬНО — импорт через индекс
import { ProductCard, useProducts } from '@/features/products';
import { Button, Modal } from '@/shared/components/ui';

// ❌ НЕПРАВИЛЬНО — прямой импорт внутренностей
import { ProductCard } from '@/features/products/components/ProductCard';
import { formatPrice } from '@/features/products/utils/formatters';
```

### Зависимости между модулями

```
features/auth     → shared (OK)
features/products → shared (OK)
features/orders   → shared (OK)
features/orders   → features/products (OK, если явная зависимость)

features/products → features/auth (НЕЖЕЛАТЕЛЬНО)
shared            → features/* (ЗАПРЕЩЕНО!)
```

---

## 📐 ПРИНЦИПЫ ПРОЕКТИРОВАНИЯ

### SOLID

1. **Single Responsibility** — один файл/класс = одна ответственность
2. **Open/Closed** — открыт для расширения, закрыт для изменения
3. **Liskov Substitution** — наследники заменяют родителей
4. **Interface Segregation** — маленькие специфичные интерфейсы
5. **Dependency Inversion** — зависимость от абстракций

### Composition over Inheritance

```typescript
// ❌ Избегай глубокого наследования
class AdminUser extends User extends BaseEntity { ... }

// ✅ Используй композицию
interface User {
  id: string;
  profile: UserProfile;
  permissions: UserPermissions;
}
```

### Инверсия зависимостей

```typescript
// ✅ ПРАВИЛЬНО — зависимость от абстракции
interface PaymentGateway {
  processPayment(amount: number): Promise<PaymentResult>;
}

class OrderService {
  constructor(private paymentGateway: PaymentGateway) {}
}

// Реализации
class StripeGateway implements PaymentGateway { ... }
class PayPalGateway implements PaymentGateway { ... }
```

---

## 🚀 ВЫБОР АРХИТЕКТУРЫ

### Критерии выбора размера проекта

| Критерий | Малый (A) | Средний (B) | Крупный (C) |
|----------|-----------|-------------|-------------|
| Срок разработки | 1-3 мес | 3-6 мес | 6+ мес |
| Команда | 1-2 чел | 2-5 чел | 5+ чел |
| Кол-во фич | 5-15 | 15-50 | 50+ |
| Сложность домена | Низкая | Средняя | Высокая |
| Масштабирование | Не нужно | Возможно | Обязательно |

### Рекомендации по выбору

```markdown
## Выбор размера проекта

**Проект:** [название]
**Оценка:** [A/B/C]

**Обоснование:**
- Срок: [X месяцев]
- Команда: [Y человек]
- Основные фичи: [список]
- Масштабирование: [нужно/не нужно]

**Рекомендуемая структура:** [ссылка на размер]
```

---

## 📋 CHECKLIST АРХИТЕКТУРЫ

### Перед началом проекта
- [ ] Определён размер проекта (A/B/C)
- [ ] Создана структура папок
- [ ] Настроены алиасы импортов (@/)
- [ ] Создан docs/01-ARCHITECTURE.md
- [ ] Определены границы модулей

### При добавлении модуля
- [ ] Модуль в правильной папке (features/shared)
- [ ] Есть index.ts с публичным API
- [ ] Внутренние детали не экспортируются
- [ ] Зависимости идут в правильном направлении

---

**Версия:** 1.0
**Дата:** 2025-01-31
